# Prod: `docker compose -f docker-compose.prod.yml up --build`
services:
  web:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.90'
          memory: 1G
