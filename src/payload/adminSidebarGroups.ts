/**
 * Группы в сайдбаре Payload Admin (коллекции / глобалы / блоки из плагинов).
 *
 * Префиксы «1.»…«5.» задают нужный порядок: Payload сортирует группы по строке имени лексикографически.
 */
export const PAYLOAD_ADMIN_GROUPS = {
  homeGlobals: '1. Главная',
  /** CMS-страницы (/slug), категории и товары каталога — одна секция сайдбара. */
  pagesAndCatalog: '2. Страницы и каталог',
  media: '3. Медиа',
  forms: '4. Формы и заявки',
  system: '5. Администрирование',
} as const;
