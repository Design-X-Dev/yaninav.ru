/** Данные секции «Контакты» после слияния CMS и дефолтов (все строки заполнены). */
export type ContactSectionContent = {
  heading: string;
  intro: string;
  contactInfoHeading: string;
  address: string;
  hours: string;
  phoneDisplay: string;
  phoneHref: string;
  emailDisplay: string;
  emailHref: string;
  formHeading: string;
  appointmentButtonText: string;
  appointmentNote: string;
};
